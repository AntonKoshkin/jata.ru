mixin cars(img)
	if attributes.mods
		if typeof attributes.mods === 'string'
			-var mods = 'cars--' + attributes.mods
		else
			-var mods = []
			each mod, i in attributes.mods
				-mods[i] = 'cars--' + mod

	.cars
		img.cars__img(
			src=img
			alt='')