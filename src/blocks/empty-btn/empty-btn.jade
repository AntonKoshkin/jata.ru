mixin emptyBtn(text)
	if attributes.mods
		-var mods = []
		each mod, i in attributes.mods
			-mods[i] = 'empty-btn--' + mod

	.empty-btn(class=mods)
		button.empty-btn__btn(
			data-next-block=attributes['data-next-block']
			data-link=attributes['data-link']
			type='button')=text