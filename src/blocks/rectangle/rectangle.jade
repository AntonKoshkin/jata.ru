mixin rectangle
	if attributes.mods
		if typeof attributes.mods === 'string'
			-var mods = 'rectangle--' + attributes.mods
		else
			-var mods = []
			each mod, i in attributes.mods
				-mods[i] = 'rectangle--' + mod

	.rectangle(class=mods)
		.rectangle__content
			if block
				block